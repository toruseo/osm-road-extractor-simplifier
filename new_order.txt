ここにあるのは，OSMデータを軽量化するための古いコードです．これを以下のように近代化してください．

読み込むファイルは，.osm.pbf形式とする

出力するファイルは，geojson形式とする．
motorway, trunk, primary, secondary別に出力するとする（例：osm_motorway.geojson）．

属性の補完処理に細かいバグがあるようなので，機能を変えずに修正できるならする